## Example Input
The function does not take any input parameters. It simply calls the endpoint `/api/v1/me` and returns the parsed response. 
## Example Output
The output of the function is an associative array that contains information about the currently authenticated user, such as their ID, name, email, and other details returned by the API. `getMe()`
